{
  "name": "sf-evenhub-proxy",
  "version": "0.1.0",
  "description": "CORS proxy that serves Salesforce data to the Even Hub G2 app. Auths via SFDX_AUTH_URL (jsforce) on Heroku, or the local sf CLI session in development.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsforce": "^3.6.4"
  }
}
